
/**
 *_abs - get the absolute value of a number
 *@n: integer to be calculated it's absolute value
 *Return: the absolute value of n
*/
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
